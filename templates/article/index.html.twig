{% extends 'base.html.twig' %}

{% block body %}
    <div class="card">
        <div class="card-body">
            <h1>{{ article.title }}</h1>
            <div>
                {{ article.text }}
            </div>
        </div>
    </div>
{% endblock %}